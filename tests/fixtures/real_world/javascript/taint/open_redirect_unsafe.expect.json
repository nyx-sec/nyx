{
  "description": "Open redirect: user-controlled URL from req.query flows to res.redirect",
  "tags": ["taint", "ssrf", "express"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-open-redirect",
      "severity": null,
      "must_match": true,
      "line_range": [4, 7],
      "evidence_contains": [],
      "notes": "req.query.next flows directly to res.redirect without validation"
    }
  ]
}
